{
  "image": "ubuntu:22.04",
    "features": {
        "ghcr.io/wxw-matt/devcontainer-features/command_runner:latest": {
			"command1": "apt update && apt upgrade",
			"command2": "apt install curl",
      "command3": "curl -L https://foundry.paradigm.xyz | bash",
      "command4": "source /root/.bashrc",
      "command5": "apt install git",
      "command6": "foundryup"
        }
    }
}
     
